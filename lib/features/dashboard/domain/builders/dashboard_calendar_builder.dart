import '../../../calendar/domain/models/calendar_view_model.dart';

class DashboardCalendarBuilder {
  const DashboardCalendarBuilder();

  CalendarViewModel build(
    CalendarViewModel calendar,
  ) {
    return calendar;
  }
}
